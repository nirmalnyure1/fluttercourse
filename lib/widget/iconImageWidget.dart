import 'package:flutter/material.dart';

class IconImageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.add_alert,size: 100,color: Colors.green,);
  }
}
